/** Host half intentionally has no services; its package metadata exposes the browser bundle. */
export function apply(): void {
  // Desktop behavior lives in the sandboxed browser half and Electron preload.
}
